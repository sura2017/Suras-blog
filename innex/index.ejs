<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
</head>
<body class="home">

    <%- include('partials/nav') %>

    <!-- HERO SECTION -->
    <div class="container" style="text-align: center; margin-top: 40px; padding: 60px 20px; background: linear-gradient(135deg, #ffffff 0%, #f1f5f9 100%);">
        <h1 style="font-size: 3rem; margin-bottom: 20px; color: #1e293b;">
            Welcome to <span style="color: var(--primary-color);">Sura Blog</span>
        </h1>
        <p style="font-size: 1.2rem; color: #64748b; max-width: 600px; margin: 0 auto 40px auto;">
            A professional Full-Stack Node.js platform built for sharing ideas, connecting with developers, and exploring modern web architecture.
        </p>

        <div class="quick-links">
            <a href="/blogs" class="button" style="padding: 15px 30px; font-size: 1.1rem;">Explore Articles</a>
            
            <% if (user) { %>
                <a href="/blogs/create" class="button" style="background: white; color: var(--primary-color); border: 2px solid var(--primary-color); margin-left: 10px;">Write a Post</a>
            <% } else { %>
                <a href="/auth/signup" class="button" style="background: #10b981; margin-left: 10px;">Join Community</a>
            <% } %>
        </div>
    </div>

    <!-- FEATURES GRID -->
    <div class="container" style="margin-top: 40px; background: transparent; box-shadow: none; border: none; padding: 0;">
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px;">
            
            <!-- Feature 1 -->
            <div style="background: white; padding: 30px; border-radius: 12px; box-shadow: var(--shadow-sm); border: 1px solid #e2e8f0;">
                <h3 style="color: var(--primary-color);">🔒 Secure Auth</h3>
                <p style="color: #64748b; font-size: 0.95rem;">
                    Built with JWT (JSON Web Tokens) and BCrypt encryption. Your data is safe, and sessions are managed securely via cookies.
                </p>
            </div>

            <!-- Feature 2 -->
            <div style="background: white; padding: 30px; border-radius: 12px; box-shadow: var(--shadow-sm); border: 1px solid #e2e8f0;">
                <h3 style="color: #f59e0b;">💬 Interactive</h3>
                <p style="color: #64748b; font-size: 0.95rem;">
                    Engage with content. Users can Like posts, leave Comments, and edit their own contributions in real-time.
                </p>
            </div>

            <!-- Feature 3 -->
            <div style="background: white; padding: 30px; border-radius: 12px; box-shadow: var(--shadow-sm); border: 1px solid #e2e8f0;">
                <h3 style="color: #ef4444;">👑 Role Power</h3>
                <p style="color: #64748b; font-size: 0.95rem;">
                    Advanced Role-Based Access Control (RBAC). Admins have full oversight, while Contributors own their content.
                </p>
            </div>

        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>